# Dev scratch files
scratch.ps1

# Temp cleanup utilities (not part of the tool)
DEL_ALL*.CMD

# Claude Code local settings
# Codex local/remote conversation attachments
.codex-remote-attachments/

.claude/

# Runtime artifacts (generated in ProgramData, not here, but just in case)
*.state.json
*.state.json.tmp
*.state.json.corrupt-*.json
BootUpdateCycle.log
BootUpdateCycle.*.log
BootUpdateCycle.history.json
upd.cmd.next
upd.cmd.next.sha256
upd.cmd.bak
*.incoming-*

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
