# Dev scratch files
scratch.ps1
testResults.xml

# Temp cleanup utilities (not part of the tool)
DEL_ALL*.CMD

# Claude Code machine-local settings (shared settings and skills are tracked)
# Codex local/remote conversation attachments
.codex-remote-attachments/

.claude/settings.local.json
.claude/*.local.json

# Runtime artifacts (generated in ProgramData, not here, but just in case)
*.state.json
*.state.json.tmp
*.state.json.corrupt-*.json
BootUpdateCycle.log
BootUpdateCycle.providers.log
BootUpdateCycle.providers.*.log
BootUpdateCycle.*.log
BootUpdateCycle.history.json
upd.cmd.next
upd.cmd.next.sha256
upd.cmd.bak
*.incoming-*

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
